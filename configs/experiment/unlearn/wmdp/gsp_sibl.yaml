# @package _global_
#
# GSP-SIBL on WMDP: Bilevel with GSP-weighted sampling
# Model: Zephyr-7b-beta
# Eval: wmdp_cyber/bio + mmlu
#
# Usage:
#   python src/train.py --config-name=unlearn.yaml \
#       experiment=unlearn/wmdp/gsp_sibl \
#       data_split=cyber \
#       task_name=wmdp_cyber_gsp_a5b5

defaults:
  - override /model: zephyr-7b-beta
  - override /trainer: GspSIBL
  - override /data: unlearn
  - override /data/datasets@data.forget: WMDP_forget
  - override /data/datasets@data.retain: WMDP_retain
  - override /eval: lm_eval

data_split: cyber

data:
  anchor: forget
  forget:
    WMDP_forget:
      args:
        hf_args:
          data_files: data/wmdp/wmdp-corpora/${data_split}-forget-corpus.jsonl
  retain:
    WMDP_retain:
      args:
        hf_args:
          data_files: data/wmdp/wmdp-corpora/${data_split}-retain-corpus.jsonl

eval:
  lm_eval:
    tasks:
      - wmdp_${data_split}
      - mmlu

collator:
  DataCollatorForSupervisedDataset:
    args:
      padding_side: left

trainer:
  method_args:
    gsp_interference_path: saves/unlearn/gsp_interference_wmdp_${data_split}.pt
    # logit_margin only — NPO dead from cold start
    forget_loss_type: logit_margin

task_name: ???
