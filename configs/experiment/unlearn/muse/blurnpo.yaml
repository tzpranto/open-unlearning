# @package _global_
# BLUR-NPO for MUSE unlearning — paper-correct hyperparameters
# Source: arXiv:2506.08164, Table 6 + MUSE/baselines/unlearn_{news,books}.sh
# Beta per split (Table 6): News=0.05, Books=0.4. Override per run.
# LR differs by split: News=2.5e-5, Books=1e-5. Override per run.

defaults:
  - override /model: Llama-2-7b-hf
  - override /trainer: BLURNPO
  - override /data: unlearn
  - override /data/datasets@data.forget: MUSE_forget
  - override /data/datasets@data.retain: MUSE_retain
  - override /eval: muse

data_split: News
forget_split: forget
retain_split: retain1
retain_logs_path: null

model:
  model_args:
    pretrained_model_name_or_path: muse-bench/MUSE-${data_split}_target

data:
  anchor: forget
  forget:
    MUSE_forget:
      args:
        hf_args:
          split: ${forget_split}
          path: muse-bench/MUSE-${data_split}
  retain:
    MUSE_retain:
      args:
        hf_args:
          path: muse-bench/MUSE-${data_split}
          split: ${retain_split}

eval:
  muse:
    data_split: ${data_split}
    retain_logs_path: ${retain_logs_path}
    overwrite: true

trainer:
  method_args:
    beta: 0.05        # Table 6: News=0.05, Books=0.4 (override per run)
  args:
    learning_rate: 2.5e-5    # News default; override to 1e-5 for Books
    lr_scheduler_type: constant

task_name: ???
