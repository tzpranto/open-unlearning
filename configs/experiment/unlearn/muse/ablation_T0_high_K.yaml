# @package _global_
# Ablation T0: High K/step ratio — 8r-like inner correction with proper batching
# 8r broke frontier with K=10, T=10, accum=1. This replicates the K/step ratio (10:1)
# with proper accum=32 batching: K=10 inner steps per outer step, 5 total outer steps.
# Hypothesis: frontier is set by inner convergence. K=1 never converges → rk degrades.
# K=10 fully recovers retain between perturbations → frontier shifts.
# Date: 2026-04-10

defaults:
  - override /model: Llama-2-7b-hf
  - override /trainer: SIBL
  - override /data: unlearn
  - override /data/datasets@data.forget: MUSE_forget
  - override /data/datasets@data.retain: MUSE_retain
  - override /eval: muse

data_split: News
forget_split: forget
retain_split: retain1
retain_logs_path: null

model:
  model_args:
    pretrained_model_name_or_path: muse-bench/MUSE-${data_split}_target

data:
  anchor: forget
  forget:
    MUSE_forget:
      args:
        hf_args:
          split: ${forget_split}
          path: muse-bench/MUSE-${data_split}
        max_length: 1024
  retain:
    MUSE_retain:
      args:
        hf_args:
          path: muse-bench/MUSE-${data_split}
          split: ${retain_split}
        max_length: 1024

eval:
  muse:
    data_split: ${data_split}
    retain_logs_path: ${retain_logs_path}
    overwrite: true

trainer:
  args:
    num_train_epochs: 1
  method_args:
    use_sparsity: false
    epsilon: 0.70
    T: 10
    K: 10
    eta_theta: 2e-4
    eta_in: 1e-4
    rho: 0.1
    lambda_init: 0.0
    lambda_boost_step: 0
    lambda_boost_value: 5.0
    rho_boost_value: 1.0
    gamma: 2e-4
    regularization_type: none

    forget_loss_type: npo
    npo_beta: 2.0
    simnpo_beta: 1.0
    simnpo_delta: 0.0
    elastic_net_l1_ratio: 0.5

    neuron_traces_path: null
    neuron_bitmap_path: null
    mask_th_low: 0.5
    mask_th_high: 1.0
    outer_lr_smooth: 0.0
    mask_freeze_layers: null
    outer_freeze_layers: null
    proportional_outer_lr: false
    retain_protection_layers: null
    retain_lr_multiplier: 1.0
    implicit_block_skip_layers: null

    invert_inner_mask: false
    outer_full_model: false

    gradient_projection: false
    gradient_projection_scope: layer
    projection_strength: 1.0
    projection_schedule: constant
    projection_rescale: false
    projection_layers: null

    use_implicit: false
    implicit_solver: neumann
    implicit_blockwise: true
    implicit_block_last_n_layers: 2
    implicit_block_include_attn: true
    implicit_block_include_mlp: true
    neumann_variant: richardson
    neumann_steps: 2
    neumann_mu: 1.0
    neumann_alpha_default: 0.01
    neumann_alpha_min: 1e-5
    neumann_alpha_max: 0.02
    neumann_use_probe_alpha: false
    neumann_max_growth_ratio: 5.0
    neumann_backtrack_factor: 0.5
    neumann_backtrack_max_tries: 4
    cg_iters: 10
    cg_tol: 1e-3
    cg_damping: 0.01

    use_steering: true
    steering_layers: [5, 6, 7]
    steering_coeff: 5.0
    steering_alpha: 1.0
    steering_only: false
    steering_retain_match: true
    steering_neuron_mask_path: null

    inner_repr_anchor: false
    inner_repr_alpha: 1.0
    inner_repr_layers: null

    inner_loss_type: ce
    inner_kl_temperature: 2.0
    outer_retain_loss_type: ce
    post_inner_loss_type: ce
    post_unlearn_inner_steps: 0
    post_inner_retain_only: false
    implicit_trace_guided: false
    implicit_trace_threshold: 0.5

    use_fisher_weighting: false
    inner_contrastive: false

    debug_implicit: false
    debug_save_arrays: false
    debug_stop_after_outer: null
    debug_condition_probes: 3

task_name: ???
