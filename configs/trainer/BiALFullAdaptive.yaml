defaults:
  - LoRABiAL

handler: BiALFullAdaptive
method_args:
  # Self-tuning — same adaptive params as LoRABiALAdaptive
  calibration_frac: 0.10
  conv_patience: 8
  ema_alpha: 0.15

args:
  per_device_train_batch_size: 1
  gradient_accumulation_steps: 16
  gradient_checkpointing: True
